{
  "name": "visual-story-planner",
  "version": "3.0.0",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "npx esbuild src/server.ts src/analyzer.ts src/validators.ts src/types.ts src/exporters/*.ts --bundle --platform=node --format=esm --outdir=dist --splitting",
    "build:tsc": "tsc",
    "start": "node dist/server.js",
    "dev": "npx esbuild src/server.ts src/analyzer.ts src/validators.ts src/types.ts src/exporters/*.ts --bundle --platform=node --format=esm --outdir=dist --splitting --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.3.3",
    "vitest": "^4.0.18"
  }
}
